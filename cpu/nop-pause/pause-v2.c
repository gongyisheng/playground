int main() {

	while(1) {
        // "asm" keyword
        // it indicates to the compiler that the following code 
        // will be written in assembly language instead of C
        __asm__ ("pause");
	}
    return 0;
}